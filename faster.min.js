(function(global,factory){typeof exports==="object"&&typeof module!=="undefined"?module.exports=factory():typeof define==="function"&&define.amd?define(factory):global.faster=factory()})(this,function(){"use strict";var faster=(selector="canvas",cb)=>{if(!cb){cb=selector;selector="canvas"}const canvas=document.querySelector(selector);const ctx=canvas.getContext("2d");let width;let height;let data;const size=()=>{canvas.width=Math.floor(document.body.clientWidth);canvas.height=Math.floor(document.body.clientHeight);width=canvas.width;height=canvas.height;data=ctx.getImageData(0,0,width,height)};size();const start=new Date;let index=0;const redraw=()=>{const time=(new Date-start)/1e3;ctx.putImageData(data,0,0);cb({canvas:canvas,ctx:ctx,start:start,time:time,width:width,height:height,index:index});index++;requestAnimationFrame(redraw)};redraw();window.addEventListener("resize",size)};return faster});